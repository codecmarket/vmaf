# VMAF CUDA Lever-2 — K-Buffered Device State — 2026-04-19

Completes the K-buffer migration for the three CUDA feature extractors
(VIF, motion, ADM). Each extractor now owns `K=4` slots, each with its
own `CUstream` and its own copy of the per-frame device buffers
(`accum_data` for VIF, `sad` for motion, the full `AdmBufferCuda` —
`data_buf` / dwt bands / `tmp_ref` / `tmp_dis` / `tmp_accum` /
`tmp_accum_h` / `tmp_res` — for ADM). Frame N+K blocks only on its own
slot's `consumed` event, and K frames can execute in parallel on the
GPU.

Landed as three commits for easy bisect:

```
82e16065  cuda/adm: Lever 2 — K-deep per-slot device buffers + per-slot streams
05cd039b  cuda/motion: Lever 2 — per-slot streams, K-deep SAD, kernel chain
6b1ceae3  cuda/vif: Lever 2 — K-deep slots with per-slot streams and device buffers
```

## Results

All measurements are bare wall-clock (no nsys), best-of-3 over
`crowd_run` 300 frames. Scores are bit-exact on every cell (18.8480 /
18.7314 / 23.5989 at 480p / 1080p / 2160p).

| GPU       | Res    | Baseline (fps) | Lever 2 (fps) |   Δ    |
|-----------|-------:|---------------:|--------------:|-------:|
| RTX 5070  |  480p  |         636.94 |        635.59 |  -0.2% |
| RTX 5070  | 1080p  |         254.67 |        225.06 | -11.6% |
| RTX 5070  | 2160p  |          62.58 |         90.47 | **+44.6%** |
| RTX 4070  |  480p  |         683.37 |        686.50 |  +0.5% |
| RTX 4070  | 1080p  |         258.62 |        235.66 |  -8.9% |
| RTX 4070  | 2160p  |          66.84 |         80.78 | **+20.9%** |

The 2160p row is the headline. Blackwell 5070 now beats Ada 4070 at
2160p (+12%), restoring the GPU ordering the baseline had inverted.

## Measurement correction vs. Lever-1 / baseline docs

The baseline and Lever-1 numbers in
`baseline-2026-04-19.md` / `kbuffer-2026-04-19.md` were captured under
`nsys profile --trace=cuda,nvtx,osrt`. At 480p and 1080p the nsys
overhead (per-call CUDA API interception + OSRT tracing) dominates
the measurement — bare wall-clock at 480p is ~11× faster than what
nsys reported (636 fps vs 57 fps). Those two docs are still useful
for kernel-level timeline inspection, but the fps columns there
should not be compared directly to anything.

All Lever-2 numbers in this document are bare wall-clock. The
baseline row above was re-measured against the same clips on the same
machine with nsys disabled, at commit `966be8d5` (pre-Lever-1), so the
Δ column is apples-to-apples.

## Why 2160p wins and 1080p doesn't

Three regimes:

- **480p (CPU-bound):** GPU kernels finish in sub-millisecond. The
  whole frame is dominated by the host-side feature-collector +
  `write_scores` callback + picture-pool bookkeeping. Lever 2 does not
  touch any of that, so the fps is flat (±0.5%).

- **2160p (GPU-bound):** Feature-extraction kernel time per frame is
  long enough (~8–15 ms depending on feature) that running K frames in
  parallel on the GPU matters. With per-slot streams, ADM's scale-1–3
  on frame N can overlap with ADM's scale-0 dwt2 on frame N+1 (private
  buffers, no races), and VIF's scale 0..3 of frame N+1 can start
  while frame N is still in its D2H tail. Net +45% on 5070, +21% on
  4070.

- **1080p (intermediate, regression):** Kernel time per frame is
  ~1.5–2 ms — small enough that the serial baseline already kept the
  GPU mostly busy, large enough that the extra launch/synchronization
  bookkeeping from K streams (more `cuStreamWaitEvent`, more event
  records, the motion `kernel_done` chain) eats into the margin. Net
  -9 to -12%.

The 1080p regression is real and reproducible but is the case where
the pipeline had the least to gain from Lever 2 in the first place.
Recovering it would mean either (a) adapting the number of in-flight
slots to resolution (K=2 at 1080p, K=4 at 2160p), or (b) reducing the
per-frame event bookkeeping — e.g. collapsing the motion `kernel_done`
chain into a single waited event per in-flight batch. Neither is done.

> **Update 2026-04-19 (later the same day):** resolved via commit
> `201d4c04`. See [Update: 1080p regression resolved](#update-1080p-regression-resolved-commit-201d4c04)
> below — the root cause was host-side allocator churn in the vmaf
> tool, not in-flight slot count or event bookkeeping.

## Architectural notes

- **VIF:** straightforward. Each slot owns a `VifBufferCuda`
  including `accum_data`, `data`, `sum` bands. Picture-stream fences
  on prior frame's `scale-0` D2H removed because the per-slot `buf`
  means no cross-frame data race.

- **Motion:** trivially small device state (`sad` is 8 bytes). Real
  subtlety: the motion kernel has a built-in frame-to-frame aliasing
  dependency on `blur[prev]` / `blur[curr]`. This is preserved via a
  per-slot `kernel_done` event; slot N+1's kernel waits on slot N's
  `kernel_done` before firing. First frame passes through because the
  prior slot's `kernel_done` is unrecorded (so
  `cuStreamWaitEvent` returns immediately).

- **ADM:** biggest lift. Each slot gets a full `AdmBufferCuda` —
  roughly 570 MB per slot at 2160p, so ~2.3 GB total across K=4
  slots. Scale-0 `dwt2` still fires on each picture's own stream so
  the input picture can be released early, but writes into the slot's
  private `ref_dwt2` / `dis_dwt2` bands; scales 1–3, decouple, csf,
  cm, the `tmp_res` D2H, and the `write_scores` host callback all
  run on `slot->stream`. The Lever-1 picture-stream fence on
  `s->finished` is gone — the per-slot buf eliminates the race it was
  there to paper over.

## Memory budget (2160p, K=4)

Per slot per extractor (rough, 2160p 10-bit):

- VIF: ~30 MB × 4 slots ≈ 120 MB + ~11 MB accum_data × 4 ≈ 44 MB total
- motion: negligible (8 B × 4 slots)
- ADM: ~570 MB × 4 slots ≈ 2.3 GB

Comfortably inside 12 GB cards (5070 / 4070). At 8K or higher K this
would start to matter.

## Artifacts

Raw fps per run (best-of-3 extracted above):

```
baseline (commit 966be8d5, bare wall-clock):
  5070  480p:  616.02, 636.94, 591.72
  5070 1080p:  254.67, 252.74, 242.13
  5070 2160p:   62.10,  62.58,  61.16
  4070  480p:  411.52, 659.34, 683.37   # run 1 cold
  4070 1080p:  231.66, 257.73, 258.62
  4070 2160p:   59.43,  66.84,  62.84

lever 2 (commit 82e16065, bare wall-clock):
  5070  480p:  580.27, 635.59, 627.62
  5070 1080p:  217.71, 225.06, 224.89
  5070 2160p:   89.90,  90.47,  89.82
  4070  480p:  407.06, 686.50, 656.46   # run 1 cold
  4070 1080p:  235.66, 234.74, 230.24
  4070 2160p:   80.78,  76.28,  80.06
```

## Reproduce

```sh
# bare wall-clock, 3 runs per cell, 2 GPUs × 3 resolutions
for dev in 0 1; do for res in 480p 1080p 2160p; do for r in 1 2 3; do
  t0=$(date +%s.%N)
  CUDA_VISIBLE_DEVICES=$dev /home/dsummer/VMAF/libvmaf/build/tools/vmaf \
    --gpumask 0 \
    -r /tmp/vmaf-bench/clips/ref_${res}.y4m \
    -d /tmp/vmaf-bench/clips/dist_${res}.y4m \
    --model version=vmaf_v0.6.1 --frame_cnt 300 --quiet >/dev/null
  t1=$(date +%s.%N)
  awk -v a=$t0 -v b=$t1 -v d=$dev -v res=$res -v r=$r \
    'BEGIN{printf "dev=%s res=%s run=%s elapsed=%.3fs fps=%.2f\n", d, res, r, b-a, 300/(b-a)}'
done; done; done
```

## Update: 1080p regression resolved (commit `201d4c04`)

After landing Lever 2 we traced the 1080p slowdown to host-side
allocator churn, not to the K-stream bookkeeping that the "Why 2160p
wins and 1080p doesn't" section above hypothesised. The bookkeeping
was a red herring — it cost tens of microseconds per frame, not
hundreds.

### The actual mechanism

`fetch_picture` in the vmaf tool was calling `vmaf_picture_alloc` +
implicit `aligned_free` on every frame of both ref and dist streams.
The host-side Y4M buffer is ~9 MB at 1080p, which exceeds glibc's
default `M_MMAP_THRESHOLD` (128 KB), so each alloc/free pair is a
direct `mmap` / `munmap` — a full page-table edit plus a zero-fill.

On the pre-Lever-2 binary this happened to land on a fast path: the
free in one `vmaf_read_pictures` and the alloc in the next
`fetch_picture` were close enough together that glibc's allocator
could reuse the just-released region. Lever 2's extra per-frame CPU
work (K-slot event records, `cuStreamWaitEvent` per slot, feature
collector bookkeeping) pushed the interval past whatever heuristic
glibc uses and dropped it into the slow path. Instrumenting
`fetch_picture` showed alloc time going from ~330 ms to ~430 ms over
600 calls — exactly the missing ~90 ms from the 1080p wall-clock
delta.

### The fix

`tools/vmaf.c` now pre-allocates a ring of two `VmafPicture`s per
stream and hands them out via `vmaf_picture_ref`. Depth 2 is
sufficient because the only carry-over between `vmaf_read_pictures`
calls is `vmaf->prev_ref` on the ref stream, which holds slot `i-1`
until the next call releases it — so slot `i` is always free by the
time the ring wraps back to it.

Host data is safe to overwrite after `vmaf_read_pictures` returns
because `cuMemcpy2DAsync` from pageable host memory stages through a
pinned staging buffer synchronously inside the API call; the "Async"
only refers to the device-side copy after that point.

The library itself is unchanged. The fix is 57 lines in
`tools/vmaf.c`.

### Results (best-of-3, bare wall-clock, same clips, same commit stack)

| GPU       | Res    | Baseline (fps) | Lever 2 (fps) | Ring fix (fps) |   Δ vs baseline |
|-----------|-------:|---------------:|--------------:|---------------:|----------------:|
| RTX 5070  |  480p  |         636.94 |        635.59 |         657.83 |          +3.3%  |
| RTX 5070  | 1080p  |         254.67 |        225.06 |         345.52 |     **+35.7%**  |
| RTX 5070  | 2160p  |          62.58 |         90.47 |          97.62 |     **+56.0%**  |
| RTX 4070  |  480p  |         683.37 |        686.50 |         702.60 |          +2.8%  |
| RTX 4070  | 1080p  |         258.62 |        235.66 |         347.37 |     **+34.3%**  |
| RTX 4070  | 2160p  |          66.84 |         80.78 |          98.99 |     **+48.1%**  |

Bit-exact on every cell (18.8480 / 18.7314 / 23.5989).

The ring wins every cell — not just 1080p. At 2160p it adds another
~8–22% on top of Lever 2 by removing the same per-frame mmap/munmap
cost; at 480p the remaining work is in `write_scores` / feature
collector and the gain is small.

### Raw fps per run

```
ring (commit 201d4c04, bare wall-clock):
  5070  480p:  613.60, 638.16, 657.83
  5070 1080p:  344.23, 345.52, 344.87
  5070 2160p:   97.62,  96.30,  95.91
  4070  480p:  463.95, 682.43, 702.60   # run 1 cold
  4070 1080p:  347.37, 345.80, 342.46
  4070 2160p:   98.82,  98.77,  98.99
```

### Why the original hypothesis was wrong

The "Why 2160p wins and 1080p doesn't" section above attributed the
regression to K-stream bookkeeping (`cuStreamWaitEvent`, event
records, motion's `kernel_done` chain). Those cost on the order of
10 µs per frame per feature — ~9 ms over a 300-frame run, not 120 ms.
The bookkeeping hypothesis was only plausible because we hadn't yet
instrumented `fetch_picture` itself.

Concrete instrumentation recipe (for future regressions): add
`clock_gettime(CLOCK_MONOTONIC, ...)` brackets around `fetch_picture`
body, `vmaf_read_pictures` body, and the main-loop post-amble, with
file-scope accumulators and a dump at the 600th fetch call. Segment
deltas between runs make allocator vs. CUDA-side costs trivially
separable.
